# Automatic HTTPS (Let's Encrypt) reverse proxy for the Laro app container.
# LARO_DOMAIN must be a public DNS name that points to this VPS's IP
# (e.g. the OVH hostname vps-xxxx.vps.ovh.net, or your own domain).
{$LARO_DOMAIN} {
	encode gzip
	reverse_proxy app:8001
}
